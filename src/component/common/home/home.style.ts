import styled from 'styled-components'

export namespace HomeStyle {
    export const Container = styled.div`
        position: fixed;
        top: 0;
        right: 0;
        margin: 20px;
        cursor: pointer;
    `
}
